# Frontend

This is the React + TypeScript frontend for the Enfos Reporting Portal.

See the [project README](../README.md) for setup, architecture, and how to run the full stack with Docker.

## Quick start (local dev)

```bash
npm install
npm run dev
```

Requires the backend running on `http://localhost:8080` (or use `docker compose up`).
